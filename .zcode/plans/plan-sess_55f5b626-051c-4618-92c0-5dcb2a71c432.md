# Plan: README aktualisieren + alle Kommentare auf Englisch

## Teil 1: README.md komplett neu schreiben (Englisch)

Der aktuelle README beschreibt das alte "FastAPI-Starter-Kit" – das Projekt ist inzwischen **Fuchser**, eine AI-Deep-Research-App. Der README wird auf Englisch neu geschrieben mit dem tatsächlichen Stand:

- **Projektbeschreibung**: Fuchser = AI Deep-Research Assistant. LangGraph-Multi-Agent (Supervisor → parallele ReAct-Researcher mit Tavily/DuckDuckGo → Synthesizer → Critic-Loop), Deep-Report-Modus (30+ Seiten: Outline-Genehmigung → kapitelweise Recherche → sequenzielle Chapter-Writer → APA/IEEE-Bibliographie), LLM-Provider umschaltbar (DeepSeek ↔ Z.ai GLM), Dokument-Upload (PDF/DOCX/TXT/MD/CSV) als Agenten-Kontext, Follow-up-Konversationen, PDF-Export, Live-SSE-Streaming, Agent-Graph-Visualisierung (@xyflow/react), JWT-Auth mit Rollen user/admin.
- **Architektur-Grafik**: aktualisiert – Browser → nginx/Vite-Proxy → FastAPI :8000 → Postgres (App-Tabellen + LangGraph-Checkpointer) + Redis (Queue + PubSub) + separater Worker-Prozess.
- **Projektstruktur**: echte Struktur inkl. `app/agent/`, `app/worker.py`, `app/core/{redis,seed}.py`, 14 Alembic-Migrationen, `backend/scripts/`, echte Frontend-Routes (landing, research, research/:id, history, …) und lib-Dateien (sse.ts, citations.ts, research.ts). Entfernt: nicht existierendes `legacy/`, falscher Root-Name `awesome-project/`.
- **Quick Start (lokal)**: Postgres + Redis via Docker, `uv sync`, Alembic-Migration, `uvicorn app.main:app --reload`, separater Worker (`python -m app.worker`), Frontend `npm run dev` mit Vite-/api-Proxy. Hinweis: Research-Runs brauchen Redis + Worker.
- **Production-Deploy**: `docker compose up` ist ein Coolify-Production-Stack (5 Services: db, redis, backend, worker, frontend-nginx; Ports nur intern, docs in prod aus). Wird ehrlich als Production-Deploy beschrieben, inkl. CORS-Default `https://fuchser.sergejgorochow.de`.
- **Häufige Befehle**: nur Befehle, die wirklich funktionieren – Tests/Lint lokal via `uv run pytest` / `uv run ruff` (Backend-Image ist `--no-dev`, hat kein pytest), Frontend-Build lokal via `npm run build` (nginx-Container hat kein node).
- **Env-Variablen**: komplette Liste inkl. Agent-Section (`DEEPSEEK_API_KEY`, `TAVILY_API_KEY`, `LLM_PROVIDER`, `GLM_API_KEY/MODEL/BASE_URL`, `REDIS_URL`).
- **Tests**: 8 Dateien / 82 Tests (Auth, Research, Agent-Graph, Deep-Report, LLM, Documents, Language) gegen SQLite in-memory.
- Lernpfad- und "Erweiterungen"-Sektionen entfallen bzw. werden durch eine kompakte Feature-/Architecture-Doku ersetzt; Lizenz-Zeile (MIT) bleibt als Einzeiler.

Kleinere mitgeräumte Stale-Bestandteile: `backend/pyproject.toml` `description` (noch "Starter-Kit", deutsch) → englische Fuchser-Beschreibung; stale "Starter-Kit"-Docstrings in `main.py` werden in Teil 2 ohnehin übersetzt/aktualisiert.

## Teil 2: Alle Kommentare & Docstrings Deutsch → Englisch (~102 Dateien)

**In Scope (übersetzen):**
- Backend Python (66 Dateien): `app/` (core, models, schemas, services, api, agent, main, worker), `tests/` (8 Dateien), `alembic/` (env.py + 14 Migrationen), `scripts/` (inkl. `test_citations.mjs` //‑Kommentare)
- Frontend (26 Dateien): alle `//`, `{/* */}` und CSS-Kommentare in `src/` plus Config-Kommentare in `vite.config.ts`, `tailwind.config.js`, `postcss.config.js`
- Infra/Config: `docker-compose.yml`, `.env.example`, `backend/Dockerfile`, `frontend/Dockerfile`, `.dockerignore`-Dateien, `backend/alembic.ini`, `backend/pyproject.toml`, `.gitignore`

**Explizit OUT of Scope (bleiben deutsch – funktional):**
- LLM-Prompts (supervisor.py, critic.py, synthesizer.py, deep_report.py, language.py) – sie steuern die deutsche Ausgabe
- API-Fehlermeldungen (`HTTPException detail`), Frontend-UI-Texte (JSX/Labels), Log-Meldungen
- Deutsche Textverarbeitungs-Logik (Regexes in research_service.py, Quellen-Keywords in synthesizer.py)
- Test-Fixtures/Assertions mit deutschem Inhalt (z. B. `"Lücke 1?"`, `"Übersetzter deutscher Text [1]."`)

**Vorgehen:** Qualitätsgleich übersetzen – die Lern-Kommentare erklären *warum*, nicht nur *was*; das bleibt erhalten. Technische Begriffe (Token, Refresh, Checkpointer, Layer) bleiben natürlich Englisch. Datei-für-Datei, gebatcht nach Verzeichnis (core → models → services → api → agent → worker → tests → alembic → scripts → Frontend → Infra). Verweise wie "Stufe 4, PLAN.md" bleiben inhaltlich erhalten, nur die Sprache wechselt.

## Teil 3: Verifikation

- Backend: `uv run ruff check .` (Lint) + `uv run pytest` (alle 82 Tests grün) – beweist, dass keine funktionalen Strings angefasst wurden
- Frontend: `npm run build` (tsc -b && vite build) – TypeScript kompiliert
- Abschließender Grep-Sweep nach restlichen deutschen Kommentar-Zeilen (Umlaut-/Wortmuster-Scan im Kommentar-Kontext), ausgenommen die bekannten funktionalen deutschen Strings
- `git diff`-Stichprobe pro Batch: geänderte Zeilen dürfen nur Kommentare betreffen

## Offene Entscheidungen (per Default so umgesetzt)

- README-Sprache: **Englisch** (konsistent mit englischen Kommentaren, öffentlicher Repo-Standard) – auf Wunsch einfach auf Deutsch umschaltbar
- Scope: **nur Kommentare/Docstrings** – Logs, Fehlermeldungen, UI- und Prompt-Texte bleiben deutsch